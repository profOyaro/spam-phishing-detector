services:
  streamlit:
    build: .
    ports:
      - "8501:8501"
    env_file:
      - .env.example
  api:
    build: .
    command: uvicorn backend.main:app --host 0.0.0.0 --port 8000
    ports:
      - "8000:8000"
    env_file:
      - .env.example
